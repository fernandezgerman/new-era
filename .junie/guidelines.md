Stack: PHP 8.2 + Laravel 12 + React-Query frontend.
Reglas:
- Usa PHP 8.2 features (attributes, enums, etc.).
- Legacy PHP 7.0 + SOAP solo en carpetas /SoapServices/.
- Docker-aware: considera docker-compose.yml.

